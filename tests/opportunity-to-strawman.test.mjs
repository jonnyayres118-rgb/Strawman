import test from "node:test";import assert from "node:assert/strict";import {architect} from "../lib/architect.mjs";import {engagementFromOpportunity} from "../lib/engagement.mjs";
test("Build Strawman preserves opportunity identity and a one-person recommendation",()=>{const o={company:"Example Co",trigger:"AI opportunity",unknown:"Unclear whether this needs a larger team, discovery needed",whyElastic:"Prove the first automation outcome"};const r=architect(o);const e=engagementFromOpportunity(o,r);assert.equal(e.account,"Example Co");assert.equal(e.outcome,"Prove the first automation outcome");assert.equal(e.scenarios[0].members.length,1);});
