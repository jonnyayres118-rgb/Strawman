"use client";
const money=n=>new Intl.NumberFormat("en-GB",{style:"currency",currency:"GBP",maximumFractionDigits:0}).format(n||0);
export default function ClientProposal({proposal,onCopyChange}){return <article className="client-proposal"><span className="eyebrow">CLIENT PROPOSAL</span><h1>{proposal.account}</h1><h2>Outcome</h2><textarea value={proposal.copy?.outcome||proposal.outcome||""} onChange={e=>onCopyChange?.("outcome",e.target.value)}/><h2>Approach</h2><textarea value={proposal.copy?.approach||proposal.approach||""} onChange={e=>onCopyChange?.("approach",e.target.value)}/><h2>Team</h2><div>{(proposal.team||[]).map((m,i)=><p key={i}><strong>{m.role}</strong>, {m.layer}, {m.daysPerWeek} days/week, {m.weeks} weeks</p>)}</div><h2>Investment</h2><p className="proposal-investment">{money(proposal.investment)}</p></article>}
