const numberOrNull=value=>value===''||value==null?null:Number(value);
const decorate=(member,layer,index)=>({...member,hierarchy:layer==='Core'?(index===0?'Lead':'Builder'):layer==='Flex'?'Specialist':'Scale',buyRate:null,sellRate:null,weeks:layer==='Core'?12:4,daysPerWeek:layer==='Core'?3:1});
export function buildTeamModel(team){return{core:(team.core||[]).map((m,i)=>decorate(m,'Core',i)),flex:(team.flex||[]).map((m,i)=>decorate(m,'Flex',i)),scale:(team.scale||[]).map((m,i)=>decorate(m,'Scale',i))};}
const layerEconomics=members=>{let cost=0,revenue=0,totalDays=0,complete=true;for(const m of members){const weeks=Number(m.weeks||0),dpw=Number(m.daysPerWeek||0),days=weeks*dpw;totalDays+=days;const buy=numberOrNull(m.buyRate),sell=numberOrNull(m.sellRate);if(buy==null||sell==null)complete=false;cost+=buy==null?0:buy*days;revenue+=sell==null?0:sell*days;}const gp=revenue-cost;return{cost,revenue,gp,gm:revenue?gp/revenue*100:0,totalDays,complete};};
export function calculateTeamEconomics(team){const core=layerEconomics(team.core||[]),flex=layerEconomics(team.flex||[]),scale=layerEconomics(team.scale||[]);return{core,flex,scale,initialInvestment:core.revenue,fullPotentialInvestment:core.revenue+flex.revenue+scale.revenue};}