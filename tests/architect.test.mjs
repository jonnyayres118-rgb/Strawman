import test from"node:test";import assert from"node:assert/strict";import{architect}from"../lib/architect.mjs";test("core is max 3",()=>assert.ok(architect({trigger:"acquisition integration"}).core.length<=3));test("flex is conditional",()=>assert.ok(architect({trigger:"AI automation"}).flex.every(x=>x.activation)));test("scale requires proof",()=>assert.ok(architect({trigger:"AI"}).scale.every(x=>/proved value/i.test(x.condition))));